"""Corpus Coverage Matrix (M1.1) — measurement infrastructure, not product code.

Reads the existing public Build Corpus (`fixtures/builds/public_corpus/manifest.json`)
and the existing regression suites, and reports what fraction and which categories of
real PoE2 builds Item Check can evaluate. See `docs/corpus_coverage/COVERAGE_REPORT.md`
for the generated baseline and `docs/CORPUS_COVERAGE_METHODOLOGY.md` for how to extend it.
"""
